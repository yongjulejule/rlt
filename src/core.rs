pub struct Core {
  pub args: Args,
}

// set sha-1 or sha-256
// read object
// resolve revisions
// write object
// quiet ?
// bare
// resolve .git dir (bare || separate-git-dir || work-tree)

// set_object_format

/*
setup() {
  resolve .git dir
  resolve_core_path();
  set_object_format();

  set sha-1 or sha-256

}
 */
